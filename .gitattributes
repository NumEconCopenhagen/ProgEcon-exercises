# Normalize line endings: store text as LF in the repository and check out
# LF on every platform. This keeps .py and .ipynb files consistent across
# Windows/macOS/Linux and avoids noisy CRLF/LF diffs.
* text=auto eol=lf

# Binary files: never normalize line endings or diff as text.
*.png binary
*.pdf binary
*.lyx binary
